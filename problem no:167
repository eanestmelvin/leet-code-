class Solution:
    def twoSum(self, numbers: List[int], target: int) -> List[int]:
        d = {}
        for i in range(len(numbers)):
            need = target - numbers[i]
            if need in d:
                return [d[need]+1, i+1]
                break
            d[numbers[i]] = i
